query ProductsGetList {
	products(take: 10) {
		data {
			id
			name
			description
			price
			categories {
				name
			}
			images {
				url
			}
		}
	}
}

query ProductGetById($id: ID!) {
	product(id: $id) {
		id
		name
		description
		categories {
			name
		}
		images {
			url
		}
		price
	}
}
